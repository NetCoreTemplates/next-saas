@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="@Css.H3">@ViewData["Title"]</h3>

<div class="max-w-xl">
    <div class="flex flex-col gap-4">
        <p>Your account contains personal data that you have given us. This page allows you to download or delete that data.</p>
        <p><strong>Deleting this data permanently removes your personal account, memberships, API keys, and notification history.</strong></p>
        <p>Shared organization data is not deleted. If you own an organization, transfer ownership or delete that organization before closing your account.</p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class="@Css.PrimaryButton" type="submit">Download</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="@Css.DangerButton">Delete</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
